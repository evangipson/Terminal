namespace Terminal.Constants
{
    /// <summary>
    /// A <see langword="static"/> collection of constant values used in the terminal.
    /// </summary>
    public static class TerminalCharactersConstants
    {
        /// <summary>
        /// The character that separates directories in the terminal.
        /// </summary>
        public const char Separator = '/';

        /// <summary>
        /// The character separating the directory and user input in the terminal.
        /// </summary>
        public const char Prompt = '>';
    }
}
